# pirouette

pi-based router + remote instance agents.

Current spike work:
- `docs/spikes/session-resume.md` — first phase-0 spike proving pi session persistence + resume
- `docs/spikes/event-streaming.md` — second phase-0 spike proving pi events can stream to browser-compatible WebSocket clients
